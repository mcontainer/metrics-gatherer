FROM alpine:latest
MAINTAINER Titouan Bion

ARG MESSAGE=hello
ARG PERIODIC_REFRESH=10
ENV MESSAGE=${MESSAGE}
ENV PERIODIC_REFRESH=${PERIODIC_REFRESH}

COPY periodic_sentence /

ENTRYPOINT /periodic_sentence $MESSAGE $PERIODIC_REFRESH
